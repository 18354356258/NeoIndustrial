# ── NuGet 依赖（不入库，用 nuget restore 还原）──
packages/
*.nupkg
nuget.exe

# ── 构建产物 ──
bin/
obj/
Release/
Debug/
*.dll
*.pdb
*.exe

# ── IDE / 工具 ──
.vs/
*.suo
*.user
*.csproj.user
*.DotSettings.user

# ── 运行期数据与配置（可能含口令，禁止入库）──
Data/
Logs/
*.log
*.db
*.db-journal
*.db-wal
dbConfig.json

# ── 备份 / 临时文件 ──
*bak*
*.bak
*.bak-*
temp_*
*.tmp

# ── 系统 ──
Thumbs.db
Desktop.ini
